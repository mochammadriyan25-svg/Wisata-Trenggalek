// lib/widgets/package/package_header.dart
/*/mport 'package:flutter/material.dart';
import '../../data/models/package_model.dart';

/// Header info paket — SKENARIO A: Dikosongkan.
/// Semua info (judul, durasi, destinasi, rating) sudah ditampilkan di hero overlay.
/// Widget ini tetap dipertahankan agar tidak merusak import yang sudah ada.
class PackageHeader extends StatelessWidget {
  final PackageModel pkg;
  const PackageHeader({super.key, required this.pkg});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}*/
